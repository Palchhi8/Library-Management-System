# Library-Management-System

# Library Management System (OOP in JavaScript)

## 📌 Description
This project is a simple **Library Management System** implemented using **Object-Oriented Programming (OOP)** in JavaScript. It allows users to **borrow** and **return** books, ensuring that a book is only available to one user at a time.

## 🚀 Features
- **Book Class**: 
  - `borrowBook()`: Marks the book as unavailable.
  - `returnBook()`: Marks the book as available.
- **User Class**:
  - `borrow(book)`: Adds the book to `borrowedBooks` if available.
  - `return(book)`: Removes the book from `borrowedBooks`.

## 🛠️ How to Run the Project
### **1️⃣ Open Terminal and Navigate to the Project Folder**
